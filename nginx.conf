# nginx.conf
server {
    listen 80;

    # 反向代理 → uvicorn
    location /api/ {
        proxy_pass http://127.0.0.1:8000;

        # CORS
        add_header Access-Control-Allow-Origin *;
    }

    # 健康检查也转发到 FastAPI 的 /health
    location /health {
        proxy_pass http://127.0.0.1:8000/health;
        add_header Access-Control-Allow-Origin *;
    }
}
